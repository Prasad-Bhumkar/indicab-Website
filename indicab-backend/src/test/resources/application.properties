# Use the "test" profile to ensure tests run against an in-memory database.
# This prevents tests from requiring a running MySQL instance and avoids
# permission issues when running locally.

spring.profiles.active=test
