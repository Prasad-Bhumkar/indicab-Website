{
  "scripts": {
    "dev:all": "concurrently \"cd indicab-frontend && npm run dev -- --port 5174\" \"cd indicab-backend && mvnw spring-boot:run\"",
    "dev:all:mac": "concurrently \"cd indicab-frontend && npm run dev -- --port 5174\" \"cd indicab-backend && ./mvnw spring-boot:run\"",
    "agent:auth": "npx --yes @builder.io/agent auth --local --json -c \"npm run dev:all\" --acceptSelfSigned",
    "agent:launch": "npx --yes @builder.io/agent launch --local --json -c \"npm run dev:all\" --acceptSelfSigned",
    "builder": "npm run agent:auth && npm run agent:launch"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
